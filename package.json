{
  "name": "send",
  "version": "1.0.0",
  "description": "Zero-server-storage P2P file transfer via WebRTC",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.0"
  }
}
